#include <iostream>

// TODO: Implement this function to swap two integers using pointers
void swap(int* a, int* b) {
    // 1. Store value of *a in a temp variable
    // 2. Assign value of *b to *a
    // 3. Assign temp to *b
}

int main() {
    int x = 10;
    int y = 20;

    std::cout << "Before swap: x = " << x << ", y = " << y << std::endl;

    // TODO: Call swap function. Hint: Pass addresses using &
    // swap(..., ...);

    std::cout << "After swap:  x = " << x << ", y = " << y << std::endl;

    return 0;
}
